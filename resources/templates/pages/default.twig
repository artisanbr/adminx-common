{% extends "@base/page.twig" %}
{% block content %}
    {% if customPageTemplate %}
        {{ include(customPageTemplate) }}
    {% else %}
        {{ include(getTemplate('content')) }}
    {% endif %}
{% endblock %}
